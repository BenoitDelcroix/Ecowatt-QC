# Usage

## Running the App

1. Install dependencies:
   ```bash
   uv venv
   uv pip install -r pyproject.toml
   ```
2. Launch the app:
   ```bash
   uv run streamlit run StreamlitApp-Ecowatt-QC.py
   ```

## Project Structure

See [Project Structure](structure.md) for details on the repository layout.
